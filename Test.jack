/** Test Methods */
class Test
{
    function void doStuff(Invader invader, Tank tank, SpaceInvaderGame game)
    {
        var Bullet bullet;
        let bullet = tank.fire();
        while (true)
        {
            do invader.move();
            do tank.move();
            do Sys.wait(100);
            if (bullet.getY() > -4)
            {
                do bullet.move();
            }
        }
        return;
    }
}