class Main 
{
    function void main() 
    {
        var SpaceInvaderGame game;
        // let game = SpaceInvaderGame.new();
        // do Test.doStuff(game);
        do Test.testCollisions();
        return;
    }
}