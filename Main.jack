class Main 
{
    function void main() 
    {
        var SpaceInvaderGame game;
        let game = SpaceInvaderGame.new();
        do Test.doStuff(game.getInvader(), game.getTank(), game);
        do invader.destroy();
        return;
    }
}