class Main 
{
    function void main() 
    {
        // do Logger.logString("Empty Lists", null);
        // do Sys.wait(500);
        // do TestCollisions.testCollisionsEmptyLists();

        do Logger.logString("Short Lists", null);
        do Sys.wait(500);
        do TestCollisions.testCollisions();
        return;
    }
}